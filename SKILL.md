---
name: ziniao-fba-returns
description: 通过紫鸟 CLI 从 Amazon Seller Central 抓取 FBA 退货（Returns）列表，按游标增量去重落盘为 CSV 主表，每日定时跑、幂等可续跑。适用于需要长期累积 FBA 退货数据做分析的场景。触发词：FBA 退货、退货报告、退货数据、returns 导出、退货增量。
metadata:
  version: 1.0.0
  targets: [workbuddy]
  requires:
    bins: [ziniao-cli, node]
  os: [darwin, linux, win32]
  read_only: true
---

# FBA 退货每日增量导出（紫鸟 CLI）

通过紫鸟浏览器抓取 Seller Central 的 FBA 退货列表，去重累积成一张 CSV 主表。

> **安全声明**：本技能对店铺是**只读**的。它只读取退货列表并写入本地 CSV，
> 不会对店铺做任何修改、提交或写回操作。可放心在无人工值守的定时任务中运行。

---

## 何时使用

- 需要把某个店铺的 FBA 退货数据长期沉淀成一张可分析的本地表。
- 需要每天自动拉取新增退货，且要求不重复、不遗漏。
- 用户提到「FBA 退货」「退货报告」「returns 导出」「退货增量」等诉求。

## 硬性前置条件

**以下三条不满足就直接告诉用户，不要尝试运行，也不要重试。**

| 条件 | 检查方式 | 不满足时 |
|---|---|---|
| 紫鸟浏览器已启动 | `ps aux \| grep ziniao` | 让用户先启动浏览器 |
| 目标店铺已登录 | 浏览器内该店铺处于登录态 | 让用户先登录 |
| ZClaw Bridge 已连接 | `lsof -nP -iTCP -sTCP:LISTEN \| grep ziniao` | 让用户检查桥服务 |

失败是**设计行为**，不是故障：去重键 + 游标保证幂等，次日续跑不会漏数据。

---

## 快速开始

```bash
# 1. 配置（脚本不含任何店铺信息，必须自己提供）
cp config.example.json config.json
#    编辑 config.json 填入 storeId / storeName
#    也可用环境变量：FBA_STORE_ID / FBA_STORE_NAME / FBA_CLI_PATH / FBA_MARKETPLACE_URL

# 2. 跑一次每日增量
node daily_pull.js
```

`config.json` 字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| `storeId` | 是 | 紫鸟店铺 ID，例如 `12345678901234` |
| `storeName` | 是 | 紫鸟中的店铺显示名，用于打开店铺 |
| `cliPath` | 否 | 紫鸟 CLI 路径，默认 `/opt/homebrew/bin/ziniao-cli` |
| `marketplaceUrl` | 否 | 退货列表页，默认美站 `/fba-return` |

---

## 核心机制：游标增量（必须先理解，否则会误判结果）

界面列表按**退款日倒序**排列，顶部永远是最新的记录。

每次运行从顶部往下翻页，一旦在当前页重新遇到「上一次运行时的最新订单号」，
就说明抵达上次边界 —— **这一行以上的全部是新增**，到此停止翻页。

```
打开列表 → 读取上次游标 → 逐页提取 → 本页出现游标ID?
                              ↓否           ↓是
                          点击下一页      停止翻页 → 去重 → 落盘 → 更新游标
```

两个可直接用于判断的性质：

1. **命中页码 = 增量规模的滞后指标**。第 1 页就命中 → 新增不足 1000 条；
   第 2 页命中 → 新增在 1000～2000 之间。
2. **停止条件由游标决定，与条数无关**。所以「今天只抓到几十条」不代表漏抓。

---

## 输出文件

| 文件 | 说明 |
|---|---|
| `master.csv` | 累计去重主表，13 列，全量历史沉淀 |
| `<日期>导出增量数据_<退款日分布>.csv` | 当日新增日报；当天无新增则不生成 |
| `daily_state.json` | 游标状态，勿手改 |
| `master.csv.good.bak` | last-known-good 自愈快照 |

主表 13 列：Marketplace / Order ID / Image / Title / ASIN / Seller SKU /
Return Reason / Authorization Date / Refund Date / Unit Received Date /
Disposition / Status / Action

**去重键 = 订单号 + ASIN**，一条退货事件只记一次。
后续状态变化不会重写已存行，因此这是**事件流水表，不是状态跟踪表**。

### 日报命名规则

`<北京时间 M月D日>导出增量数据_<MM-DD> (N 条) + <MM-DD> (M 条).csv`

例：`9月2日导出增量数据_08-31 (548 条) + 09-01 (1115 条).csv`

- 统计维度是 **Refund Date（退款日）**，多天按日期升序、` + ` 连接。
- 斜杠在 Unix 文件名中是路径分隔符，已统一改写作连字符（`08/31` → `08-31`）。
- 同一天重跑且分布撞车时，自动追加 ` (2)` 后缀，**不会静默覆盖**已有日报。

---

## 数据判读要点（最容易误判的地方）

> **不要根据新增条数判断有没有漏抓。**

每日新增量波动极大是**正常的**。实测同一店铺：一天新增 26 条，次日新增 1663 条，
相差 64 倍，两次数据都健康。更极端的一次：同一天内相隔 1.5 小时重跑，又补登了 190 条。

根因是 **Amazon 退货列表存在录入延迟**：某个退款日的记录并非当天全部进入列表，
往往次日甚至更晚才补齐。当天抓得少，只是当时列表里还没有，后续会自然补登。

### 每次跑完固定做三项检查

1. **日报内部重复数** —— 应为 0。不为 0 说明去重键失效。
2. **主表列数异常行数** —— 应为 0。脚本写后已自动校验并打印。
3. **日报 Refund Date 分布** —— 应集中在最近 1～2 天。出现大量陈旧日期说明游标异常。

> **Authorization Date 不能用来判断增量边界。**
> 它可以比退款日早数周，这是「授权早、退款晚」的正常业务形态。
> 界面筛选器走的是 `REFUND_DATE_FILTER_GROUP`，只有退款日才是排序与切片依据。
> 用 Authorization Date 判断增量会得到完全错误的结论。

---

## 安全机制与退出码

写入路径上有五道检查，任一不通过就中止，**绝不静默损坏数据**：

| 时机 | 检查 | 不通过时 |
|---|---|---|
| 读入主表前 | 是否二进制、表头是否 13 列 | 从 good.bak 自动恢复 |
| 提取每行时 | 列数必须等于 13 | 退出码 3 |
| 落盘前 | 内容是否二进制 | 退出码 4 |
| 写入前一刻 | 主表 mtime 是否被外部改动 | 放弃写入，退出码 5 |
| 写入之后 | 主表与日报逐行校验 | 打印告警 |

所有写入都是原子操作（先写临时文件再重命名），不会留下半截文件。

| 退出码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 未取得 targetId（浏览器或桥未就绪） |
| 2 | 现有 master.csv 损坏且无法自愈 |
| 3 | 提取行格式异常 |
| 4 | 待写内容疑似二进制 |
| 5 | 运行期间 master 被外部改动，已放弃写入 |
| 10 | 配置缺失（storeId/storeName 未配置，或 CLI 路径不存在） |

---

## 故障速查

| 症状 | 原因 | 处理 |
|---|---|---|
| `no targetId` | 紫鸟浏览器未启动或店铺未打开 | 启动浏览器并登录店铺后重跑 |
| 退出码 10 | 没有 config.json 或缺少 storeId | 复制 config.example.json 并填写 |
| 设置筛选失败 / 注入失败 | 页面未加载完，或前端结构变更 | 脚本内置重试；持续失败需检查页面是否改版 |
| 「首页主键未变化」 | 触达服务端行数上限 | 属正常保护，改走 `deep_scan.js` 深扫 |
| 跑满 50 页仍未命中游标 | 游标已滚出 7 天窗口 | 用 `deep_scan.js` 补登 |
| 主表被写坏 | 历史遗留问题，现已有多道防线 | 从 `master.csv.good.bak` 恢复 |
| 当天没有生成日报 | 当天确实无新增记录 | 正常，主表与游标已同步 |

---

## 其他脚本

| 脚本 | 用途 |
|---|---|
| `daily_pull.js` | **每日增量主脚本**，定时任务入口 |
| `deep_scan.js` | 30 天深扫，用于补登与回溯；每 15 页重置一次以规避卡死 |
| `validate_extract.js` | 只验证提取逻辑是否正确，不写文件 |
| `verify_csv.js` | 对已有 CSV 做严格 13 列校验 |
| `migrate_master.js` | 历史 11 列主表升级到 13 列 |
| `test_1000.js` | 分页注入连通性测试 |

---

## 已知限制

- **界面约 1 万行封顶**：单趟最多取最新约 1 万条。要拿全量历史（数十万行）
  必须走 SP-API 官方 Customer Returns 报表，本技能做不到。
- **紫鸟 F12 被企业策略禁用**：无法用开发者工具排查，改用 `page exec` 注入脚本读 DOM。
- **直接重放 refresh 请求会触发 CSRF**：返回壳页而非数据。
  必须「借页面自身的请求」—— 注入 `recordsPerPage=1000` 选项触发 change，再读渲染结果。
- **日期筛选只有 6 个预设区间**（LAST_DAY / 7 / 30 / 90 / 180_DAYS / YEAR），
  无自定义起止日期，因此无法按日期窗口切片。

---

## 定时运行

若用 WorkBuddy 自动化实现每日运行，注意：**rrule 的 BYHOUR 按本机本地时间解释，
不支持时区标识**。若本机时区与业务时区不同（例如本机 `America/New_York`，
目标北京时间 09:00），必须换算，并在夏令时切换日校正，否则会整段偏移一小时。

- 夏令时（EDT，UTC-4）：北京时间 09:00 = 本机 21:00
- 冬令时（EST，UTC-5）：北京时间 09:00 = 本机 20:00

脚本内部所有日期均按北京时间（UTC+8）计算，与本机时区无关，文件名前缀不会错。
